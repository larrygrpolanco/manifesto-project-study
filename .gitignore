# --- LLM collection: cached raw API responses (regenerable) -----------------
# One file per API call. predictions.csv (the tidy long table) IS tracked.
runs/raw/
!runs/raw/.gitkeep
runs/pilot/raw/

# --- Archived prior work: heavy/regenerable subdirs --------------------------
archive/early-experiment/data/
archive/early-experiment/pilot-1/results/raw/

# --- Secrets ----------------------------------------------------------------
*.env
.env

# --- R ----------------------------------------------------------------------
.Rhistory
.RData
.Rdata
.Rproj.user/

# --- Python -----------------------------------------------------------------
__pycache__/
*.pyc
.venv/
venv/

# --- OS / editor ------------------------------------------------------------
.DS_Store
