# --- Data (too large for git; raw downloads, subsets, codebook sources) -----
# Anchored to repo root so it does NOT match pilot-1/data/ (pilot data is tracked).
/data/

# --- Pilot-1 cached raw API responses (regenerable; predictions.csv is tracked)
pilot-1/results/raw/

# --- Secrets ----------------------------------------------------------------
# Manifesto Project API key (the .example template stays tracked)
code/R/manifesto_apikey.txt
*.env
.env

# --- R ----------------------------------------------------------------------
.Rhistory
.RData
.Rdata
.Rproj.user/

# --- Python -----------------------------------------------------------------
__pycache__/
*.pyc
.venv/
venv/

# --- OS / editor ------------------------------------------------------------
.DS_Store
